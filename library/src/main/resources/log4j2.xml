<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
    <Appenders>
        <!-- Console appender -->
        <Console name="StdOut" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS}  %-5level  %msg%n"/>
        </Console>

        <!-- Rolling file appender for general logs -->
        <RollingFile name="RollingFile" fileName="logs/debug.log"
                     filePattern="logs/debug.%d{yyyy-MM-dd}.log">
            <PatternLayout pattern="%d{HH:mm:ss.SSS}  %-5level  %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>

        <!-- Rolling file appender for EZSP logs -->
        <RollingFile name="EzspFile" fileName="logs/ezsp.log"
                     filePattern="logs/ezsp.%d{yyyy-MM-dd}.log">
            <PatternLayout pattern="%d{HH:mm:ss.SSS}  %-5level  %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- ZigBee library logging -->
        <Logger name="com.zsmartsystems.zigbee" level="INFO" additivity="true">
            <AppenderRef ref="StdOut"/>
        </Logger>

        <!-- EZSP specific logging -->
        <Logger name="com.zsmartsystems.zigbee.dongle.ember.internal.ash" level="INFO" additivity="true">
            <AppenderRef ref="EzspFile"/>
        </Logger>

        <!-- Suppress commons logging -->
        <Logger name="org.apache.commons" level="OFF"/>

        <!-- Root logger -->
        <Root level="INFO">
            <AppenderRef ref="RollingFile"/>
        </Root>
    </Loggers>
</Configuration>

